spring:
  datasource:
    url: "jdbc:postgresql://${wqpDbHost}:${wqpDbPort}/wqp_core"
    username: ${wqpDbUsername}
    password: ${wqpDbUserPassword}
    driver-class-name: org.postgresql.Driver
      
  profiles: 
    active: default,swagger
